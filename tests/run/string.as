extern def puts(str);
def main() {
    # Declare some strings.
    let empty: str = '';
    let single_quote: str = "'";
    let double_quote: str = '"';
    let line: str = "Hello World, this is some text.";

    # Print the strings.
    puts(empty);
    puts(single_quote);
    puts(double_quote);
    puts(line);
}
