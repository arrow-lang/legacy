# [ ] move `Generator.nodes` to a `nodes` property on the function handle
# [ ] `extract_attached_function` needs to store the attached function with
#     the attached type_ node.

def main() {
    def nested() {
    }
    let m = Point();
}

struct Point { }

# [ ]
implement (int, bool) {
    first(self) -> self[0];
    second(self) -> self[1];
}
