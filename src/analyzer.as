# import ast;

# # Semantic analyzer
# # -----------------------------------------------------------------------------
# # Analyze and markup the AST using various passes.

# def analyze(mut node: ast.Node) {

#     # Analyze literal expressions and determine their type in context.
#     # Note that literal expressions can only have their type resolved
#     # if their
#     analyze_literal(node);

# }

